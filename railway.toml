[build]
builder = "nixpacks"
buildCommand = "cd backend && npm install && npm run build"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["nodejs_24"]

[deploy]
startCommand = "cd backend/build && node bin/server.js"
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
